# --- PULL (download changes from GitHub into your local folder) ---
git pull origin main --allow-unrelated-histories

# --- PUSH (upload your local changes to GitHub) ---
git add .
git commit -m "Update project files"
git push origin main
